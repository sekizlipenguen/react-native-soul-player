import SoulPlayer from './src/components/SoulPlayer';
import {resolveLabels, SUPPORTED_LOCALES, normalizeLocale} from './src/i18n';
import {DEFAULT_CONTROLS, mergeControls} from './src/defaults/controls';

export default SoulPlayer;
export {
  SoulPlayer,
  resolveLabels,
  SUPPORTED_LOCALES,
  normalizeLocale,
  DEFAULT_CONTROLS,
  mergeControls,
};
